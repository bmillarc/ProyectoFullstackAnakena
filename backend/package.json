{
  "name": "anakena-backend",
  "version": "1.0.0",
  "description": "Backend simulado para la aplicación web de Anakena DCC",
  "main": "server.js",
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "dev": "json-server --watch db.json --port 3001 --delay 500",
    "reset": "cp db.backup.json db.json && echo 'Base de datos restaurada'"
  },
  "keywords": [
    "json-server",
    "mock-api",
    "anakena",
    "dcc"
  ],
  "author": "Equipo 102",
  "license": "ISC",
  "dependencies": {
    "json-server": "^0.17.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}